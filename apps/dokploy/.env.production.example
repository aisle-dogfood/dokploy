DATABASE_URL="postgres://dokploy:amukds4wi9001583845717ad2@dokploy-postgres:5432/dokploy"
PORT=3000
NODE_ENV=production

# Encryption key for sensitive data at rest (passwords, tokens, API keys)
# Generate with: openssl rand -hex 32
# IMPORTANT: Store this securely using a secrets manager in production!
ENCRYPTION_KEY=""